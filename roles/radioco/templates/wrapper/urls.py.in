from django.conf.urls import include, patterns, url
from radioco.configs.common.urls import urlpatterns

urlpatterns += patterns(
    '',
    url(r'^station-data', include('radioco.frapp.urls', namespace="frapp")))
