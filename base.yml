---
- hosts: appservers
  gather_facts: False
  become: True
  tasks:
  - name: install requirements
    apt:
      name:
        - git
        - libjpeg-dev
        - libpq-dev
        - nodejs
        - npm
        - python3-dev
        - python3-pip
        - python3-virtualenv
        - supervisor

#- hosts: dbserver
#  gather_facts: False
#  tasks:
#  - name: install requirements
#    become: True
#    apt:
#      name: python3-psycopg2
